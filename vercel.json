{
  "version": 2,
  "builds": [{ "src": "*.js", "use": "@vercel/node" }],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/server"
    }
  ],
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/server"
    }
  ]
}
